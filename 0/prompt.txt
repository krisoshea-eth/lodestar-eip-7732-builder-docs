ok is there anything else that needs to be done on linear based on linears available features and our implementation p[lan? this looks pretty good but if we ccan levberage any other linear features that are useful we should based on the plan of cousre. also here is nicos response, we need to reply and consider how this impacts the plan and linear and update the plan: **nflaig** — **4:25 PM**
Would you prefer v1 to actively support both, or should next-slot preparation be the primary path, with current-slot requests only working when suitable prepared state already exists?
the question here is rather when you want to submit the bid, I think this should be configurable, it makes sense to broadcast it a bit earlier, not exactly at t=0 of the proposal slot otherwise you are racing against the proposer and you don't really know what their cutoff is when they select the bid from p2p, eg. current lodestar just select whatever it has when the local block is built, once we have the builder-api there will be some timeout, but imo it makes sense to propagate the bid a bit sooner

For v1, can preparation for the old head simply be superseded and removed through the bounded cache and expiry rules, rather than adding an explicit cancellation path? An already-published bid would remain published, and the Builder would submit a new bid for the new parent tuple.
what cache do you mean? the bn I am pretty sure already cleans up the payload cache if I am not wrong