You said above in your last response that there are three unanswered points explicitly open. What do you mean there are three?

I thought in our message response to Nico that there's only like one or two. Where are you getting that from?

I haven't sent that message to Nico yet, so are we sure this message accurately answers everything he's asked in the follow-up Discord messages? here are nicos replies again:  For the Builder execution fee recipient, should it be supplied on each candidate request or through a preparation or registration call?
you could supply it when requesting the bid probably? but I do see a problem, we need to prepare an execution payload beforehand, and also trigger this somehow, lodestar currently only does this if we have a proposer for next epoch, maybe need to extend our prepareNextSlot logic, and maybe add a new api to prepare a bid? I could imagine the builder could just say "please prepare a bid on this branch" and the bn does it in prepare next slot, would need to think more about a clean design here, there are many ways to achieve this

For the first stateful implementation, are you happy for us to reuse the existing BN production-cache model, adding the smallest secondary mapping or dedicated cache needed before a selecting block root exists? The plan assumes one source BN, no stateless or multi-BN failover in v1, and eviction after successful envelope publication.
ideally we can reuse the cache, for now I don't really mind it to be stateful only, the builder runs on the same host as the node itself as a sidecar, I wouldn't but too much effort into it right now to make it stateless and multi-bn

Should v1 actively support both current-slot and next-slot candidate requests? For the initial policy, should there be one candidate and bid per slot, or should we support replacement bids when the payload or head changes? If replacements are expected, should stale work be explicitly cancelled following a head change?
you can resubmit a bid of head changes, this could be achieve by keeping track of the head of the node via sse, but in general, at the time you submit the bid, if head is strong there is no risk that it's gonna get reorged, of course there are better strategies, if you know head is weak, you might wanna submit for parent block and head, same foes for FULL/EMPTY
4: For envelope publication, should the sidecar explicitly request consensus_and_equivocation rather than relying on the endpoint default? If the pinned Lodestar baseline still lacks the equivocation check, we will add the narrow BN validation and prove it with the Deathstar test. We also plan bounded immediate retries, recording a late outcome after PAYLOAD_DUE, rather than retrying indefinitely or introducing strategic withholding.
yes, the broadcast validation should be consensus_and_equivocation, that's a good reminder for me to finally implement that, just assume that we have it for now, this is anyways only relevant in a case if the proposer tries to unbundle the payload   plesae add links to the pr 😂
[https://github.com/ethereum/consensus-specs/pull/5497](https://github.com/ethereum/consensus-specs/pull/5497) we merged yesterday
[https://github.com/ChainSafe/lodestar/pull/9723](https://github.com/ChainSafe/lodestar/pull/9723) what the hell is this (lodekeeper hallucinated something it seems) 
ok but don't worry, these pr won't affect you
[https://github.com/ChainSafe/lodestar/pull/9739](https://github.com/ChainSafe/lodestar/pull/9739) this one is relevant as it changes how you can submit bids, but if you submit bids per head view of the beacon node the builder is connected to you are fine, this becomes more complicated if you wanna flood publish bids for different branches, for that we'd need to relax the gossip validation of the local beaocn node itself 
we run validateApiExecutionPayloadBid if you submit a bid via api, but need to relax that to support flood publishing on different branches, becasue the head view of peers can be different, but that is a stretch goal, it would be kinda nice to have a builder that can bid on these branches, that will be interesting for the non-finality testnet to see how bid propagation works, with #5497 spec change, we made it really strict, my worry is that it could limit bids from propagating correctly, but that remains to be tested 
**nflaig** — **8/1/26, 1:18 PM**
these sounds correct to me 👍    and here were your drafted replies are you sure your preplies are sufficeint and all correct before i  send them and there is nothing there that reads ai generated or weird?   Message 1
Sorry for the slow reply. It is a long weekend here in Ireland and today is a bank holiday, so apologies for the delay getting back to you. I really appreciate the detailed response, it is super helpful.
On the fee-recipient and payload-preparation flow, that makes sense. I will stop treating the exact request shape as settled. We will first trace the existing prepareNextSlot flow and work out the cleanest way for the sidecar to ask the BN to prepare a bid for a particular slot and branch, while passing the Builder payload fee recipient. We can bring the proposed route and lifecycle back to you before locking it into the implementation.
One part of that question is still open: should the execution payload’s feeRecipient be required to equal the Builder’s on-chain execution address, or can it be a separately configured address supplied with the preparation or candidate request?
Given that the payload needs to be prepared in advance, I am also thinking the next-slot flow should be the primary v1 path, with a current-slot request only working when suitable prepared state already exists. Does that match what you had in mind?
For v1 I will treat the new preparation or candidate surface as a trusted, operator-controlled interface between the same-host sidecar and BN, with proper request bounds and timeouts. We will not design it as an unauthenticated public payload-building endpoint.
On the cache, great. We will reuse the existing BN production-cache model where possible, keep the first implementation stateful with one same-host source BN, and avoid spending core time on stateless or multi-BN support.

Message 2
On head changes, I will update the plan so the core Builder tracks the connected BN’s head through SSE, submits bids compatible with that BN’s current head view, and can resubmit when the head changes. A strong-head happy path remains the initial target.
Bidding across the parent and head, handling both FULL and EMPTY branches, and flood publishing across different branch views will stay as stretch work. For v1, I am assuming a head change cancels or supersedes only local in-flight preparation. An already-published bid cannot be withdrawn, so the Builder would publish a new bid for the new parent tuple instead. Please correct me if that is not the right model.
For envelope publication, we will explicitly request consensus_and_equivocation. We will assume the BN provides that validation rather than adding equivocation policy to the sidecar, and we will retain the Deathstar test for the proposer-unbundling case. The sidecar will still use bounded immediate retries and record a late outcome after PAYLOAD_DUE.
Thanks for the PR links too 😂 I will include direct links whenever I refer to upstream work:
[consensus-specs #5497](https://github.com/ethereum/consensus-specs/pull/5497) is merged, so we will use the head-compatible bid rules.
You are right about [Lodestar #9723](https://github.com/ChainSafe/lodestar/pull/9723). I will remove it from the Builder dependency list and will not rely on it for this project.
[Lodestar #9739](https://github.com/ChainSafe/lodestar/pull/9739) has now merged as well. We will rebase onto it and align the API submission path with its head-compatible validation.
I will record the relaxed local validation and multi-branch propagation work as a possible stretch experiment for the non-finality testnet, rather than part of the first implementation. That should let us test the propagation concern from #5497 later without complicating the honest same-head path.
Thanks as well for confirming that the remaining v1 assumptions look correct. If the fee-recipient identity, next-slot-first flow, and head-change handling above sound right, I think that closes the remaining questions and I can update the plan accordingly.