# Lodestar EIP-7732 Builder Docs

This private repository is a GitHub review mirror for the Lodestar EIP-7732 Builder HackMD documents.

The initial Markdown mirror is being introduced through a review pull request so collaborators can comment inline on the raw Markdown.
